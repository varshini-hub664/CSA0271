#include <stdio.h>
int main() {
 int num = 15;
 int i;
 int count = 0;

 for(i=1; i<=num; i++)
 {
  if(num % i == 0)
   count++;
 }

 if(count > 2)
  printf("%d is a composite number", num);
 else
  printf("%d is not a composite number", num);
 return 0;
}
